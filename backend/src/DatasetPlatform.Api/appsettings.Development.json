{
  "Storage": {
    "BasePath": "../../../data",
    "SimulatedLatencyMs": 100
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ApiDebug": {
    "_comment": "Api diagnostics logging configuration.",
    "Enabled": true,
    "LogFilePath": "apiDiag.log",
    "MaxLogRows": 1000,
    "Verbosity": "Compact",
    "LogSearchEfficiencyStats": true,
    "_comment_Verbosity": "Allowed values: Full (logs full request/response bodies), Compact (omits bodies and logs only payload byte sizes).",
    "_comment_LogSearchEfficiencyStats": "When true and includeInternalInfo=true, logs extracted internalInfo.searchEfficiency stats as a single compact line.",
    "_comment_MaxLogRows": "Retains only the most recent number of rows in apiDiag.log."
  }
}
